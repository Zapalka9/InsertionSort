#ifndef SORT_H
#define SORT_H

#define LEN 12
void indexInit(int index[], int len);	// intitialisiert das Indexfeld -> 0, 1, 2, ...
void insertionSort(double data[], int index[], int len, int sorted);	// Datenfeld, Indexfeld, Anzahl Indices, Sortierreihenfolge 0=DEC  1=ASC
void insertionSort2(double data[], int index[], int len, int sorted);	// Datenfeld, Indexfeld, Anzahl Indices, Sortierreihenfolge 0=DEC  1=ASC



#endif
